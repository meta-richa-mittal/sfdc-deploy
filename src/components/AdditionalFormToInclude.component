<apex:component controller="AdditionalFormCtrl">
    <apex:repeat value="{!ImageList}" var="image">
        <div class="page-size">
            <img src="/servlet/servlet.FileDownload?file={!image.Id}" width="100%" height="100%" />
        </div>
    </apex:repeat>
</apex:component>